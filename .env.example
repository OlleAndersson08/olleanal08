# Kopiera den här filen till ".env" och fyll i din databaslänk.
# Lämnar du den tom används en lokal databas automatiskt (bra för test).

# Permanent molndatabas (Postgres). Klistra in länken från Neon eller Vercel Postgres.
# Exempel: postgresql://anvandare:losen@host.neon.tech/dbnamn?sslmode=require
DATABASE_URL=
